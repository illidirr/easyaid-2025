{
  "build": "pip install -r requirements.txt && python manage.py makemigrations && python manage.py migrate && python manage.py collectstatic --noinput",
  "start": "gunicorn EasyAid.wsgi:application --bind 0.0.0.0:8080"
}